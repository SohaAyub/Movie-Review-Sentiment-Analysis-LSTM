# Movie-Review-Sentiment-Analysis-LSTM
Movie review sentiment analysis using NLP and LSTM with the IMDB dataset. The project covers text preprocessing, tokenization, padding, word embeddings, LSTM-based classification, evaluation, custom predictions, and error analysis.
